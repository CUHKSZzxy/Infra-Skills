interface:
  display_name: "Profile Serving Timeline"
  short_description: "Profile serving GPU timelines"
  default_prompt: "Use $profile-serving-timeline to analyze an existing LMDeploy, vLLM, or SGLang serving trace, or capture one when needed."
