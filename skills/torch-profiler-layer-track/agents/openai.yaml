interface:
  display_name: "Torch Profiler Layer Track"
  short_description: "Compact GPU display lanes and add layer-number guides"
  default_prompt: "Use $torch-profiler-layer-track to add verified layer-number guides and at most ten synthetic GPU display lanes to my local Torch Profiler trace, preserving original timing and stream provenance."
